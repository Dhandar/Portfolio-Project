# URL of the contact-form API (see /server). Leave unset for local dev —
# it defaults to http://localhost:5000. Set this to your deployed API URL
# (e.g. Render/Railway URL) when you deploy the frontend.
VITE_API_URL=http://localhost:5000
